package com.banglasticker.app;

import androidx.core.content.FileProvider;

/** FileProvider used to grant temporary read access when sharing a sticker to other apps. */
public class ContentFileProvider extends FileProvider {
}
